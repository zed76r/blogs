const ghp = require('gh-pages')

ghp.publish("public", {
    branch: 'main',
    repo: `https://x-access-token:${process.env.GH_TOKEN}@github.com/zedd-dev/www.git`,
    user: {
        name: "gh-pages",
        email: "ghpages@zedd.dev"
    },
    message: `Generated by ${process.env.GITHUB_REPOSITORY}@${process.env.COMMIT_HASH}`
}, err => {
    if (err) {
        console.error(err)
        process.exit(255)
    }
})